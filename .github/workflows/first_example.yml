name: First Workflow 

on: push 

jobs:
    first_job:
        runs-on: mac-latest 
        steps:
            - name: checkout repo
              uses: actions/checkout@v5
            - name: permissions 
              run: chmod +x ./code.sh
            - name: welcome
              run: ./code.sh