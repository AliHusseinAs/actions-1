name: First Workflow 

on: push 

jobs:
    first_job:
        runs-on: ubuntu-latest 
        steps:
            - name: checkout repo
              uses: actions/checkout@v5
            - name: welcome
              run: echo "My firsrt GitHub Actions Pipeline"

            - name: List files 
              run: ls 
            
            - name: read file readme.md 
              run: cat README.md
            - name: remove file
              run: rm -r /home/runner/work/actions-1/actions-1/mock.txt